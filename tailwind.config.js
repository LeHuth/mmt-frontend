module.exports = {
    plugins: [require('daisyui'),require('@tailwindcss/typography')],
    darkMode: 'class',
  theme: {
    extend: {
      colors: {
        primary: colors.gray
      }
    }
  }
};
